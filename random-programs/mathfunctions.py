# This is a module that imports a set of familiar math functions
# you must import it, then use math.xxx to call the function
import math
degrees = 45
radians = degrees / 360.0 * 2 * math.pi
print(degrees,'degrees is', radians,'radians')
print(math.sqrt(2))